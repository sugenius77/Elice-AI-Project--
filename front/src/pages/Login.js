import Layout from "components/Layout";
import React from "react";

const Login = () => {
    return (
        <Layout>
            <h1>Login page</h1>
        </Layout>
    );
};

export default Login;
